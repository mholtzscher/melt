name: Update Dependencies

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  update-bun:
    uses: mholtzscher/github-actions/.github/workflows/update-bun-deps.yml@main
    secrets:
      token: ${{ secrets.PAT }}

  update-nix:
    uses: mholtzscher/github-actions/.github/workflows/update-nix-flake.yml@main
    secrets:
      token: ${{ secrets.PAT }}
